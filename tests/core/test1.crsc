// Some data sorts
data Boolean     ( True, False )
data ∀ a. Option ( None, Some(a) )
data ∀ a. List   ( Nil, Cons(a, List(a)) )

// Some function sorts
func :(List(String))

// Some rules
rule :(Cons(#, #s)) → Concat(#, :(#s))  