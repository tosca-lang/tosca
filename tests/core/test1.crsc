// Some data sorts
data Boolean     ( True, False )
data ∀ a. Option ( None, Some(a) )
data ∀ a. List   ( Nil, Cons(a, List(a)) )

// Some function sorts
func String Flatten( List(String) )

// Some rules
rule Flatten( Cons(#, #s) ) → Concat(#, Flatten(#s))  

// N is natural number sort
data N ( Z, S(N) )
func N Plus(N, N)
rule Plus(Z,   #2) → #2 
rule Plus(S(#1), #2) → S( Plus(#1,#2) )
