data Bool ( True, False )

func Bool And(Bool, Bool) 
rule And(True, True) → True
rule And(True, #)    → #

func Bool Main
rule Main → And(True, True)

// N is natural number sort
data N ( Z, S(N) )
func N Plus(N, N)
rule Plus(Z,   #2) → #2 
rule Plus(S(#1), #2) → S( Plus(#1,#2) )
