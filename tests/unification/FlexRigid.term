/*
 * Copyright (c) 2015 IBM Corporation.
 *  
 *
 * @author : m schett
 * @date   : 07/31/2015
 */


(

Assert[ State[Mgu[( PairCons[ "#X",  %term⟦ A ⟧]; )],0],
        FlexRigid[State[Mgu[()],0], "#X" ,(), %term⟦ A ⟧]
      ]; 

Assert[ State[Mgu[( PairCons[ "#X",  %term⟦ F[A,B] ⟧]; )],0],
        FlexRigid[State[Mgu[()],0], "#X" ,(), %term⟦ F[A,B] ⟧]
      ]; 

Assert[ State[Mgu[( PairCons[ "#X",  %term⟦ x.F[A] ⟧]; )],0],
        FlexRigid[State[Mgu[()],0], "#X" ,(%variable⟦ x ⟧;), %term⟦ F[A] ⟧]
      ]; 

Assert[ State[Mgu[( PairCons[ "#X",  %term⟦ x.F[A,B] ⟧]; )],0],
        FlexRigid[State[Mgu[()],0], "#X" ,(%variable⟦ x ⟧;), %term⟦ F[A,B] ⟧]
      ]; 

Assert[ State[Mgu[( PairCons[ "#X",  %term⟦ x y .F[y,x] ⟧]; )],0],
        FlexRigid[State[Mgu[()],0], "#X" ,(%variable⟦ x ⟧; %variable⟦ y ⟧;), %term⟦ F[y,x] ⟧]
      ]; 

Assert[ State[Mgu[( PairCons[ "#X",  %term⟦ x y .F[y,x] ⟧]; )],0],
        FlexRigid[State[Mgu[()],0], "#X" ,(%variable⟦ x ⟧; %variable⟦ y ⟧;), %term⟦ F[y,x] ⟧]
      ]; 

Assert[ State[Mgu[( PairCons[ "#G",  %term⟦ y x . #0[x] ⟧];  PairCons[ "#F",  %term⟦ x . C[#0[x]] ⟧]; )], 1],
        FlexRigid[State[Mgu[()], 0], "#F" ,(%variable⟦ x ⟧; ), %term⟦ C[#G[y,x]] ⟧]
      ]; 
)