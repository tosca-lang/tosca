/*
 * Copyright (c) 2015 IBM Corporation.
 * 
 * Tests for Intersect
 *
 * @author : m schett
 * @date   : 07/15/2015
 */

(

Assert[ (1;),
        Intersect[(1;),(1;2;)]
      ];

Assert[ (),
        Intersect[(5;),(1;2;)]
      ];

Assert[ (a;),
        Intersect[(a;a;),(a;b;)]
      ];

Assert[ (a;),
        Intersect[(a;a;),(a;a;b;)]
      ];

Assert[ (a;),
        Intersect[(a;a;),(a;a;)]
      ];
)