apply plugin: 'groovy'
apply plugin: 'maven'
 
dependencies {
    compile gradleApi()
    compile localGroovy()	
}

group = 'org.crsx'
version = '1.0'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('..'))
        }
    }
}
